import 'package:flutter/material.dart';

class FormularVerwaltungScreen extends StatelessWidget {
  const FormularVerwaltungScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [],
    );
  }
}
