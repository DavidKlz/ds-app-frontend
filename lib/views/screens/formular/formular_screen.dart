import 'package:flutter/material.dart';

class FormularScreen extends StatelessWidget {
  const FormularScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
